# Build

**Dev server:**
```bash
npm run dev
```

**Production build (validate only — don't distribute without release.md):**
```bash
npm run build
```

**Electron (packaging):**
```bash
npm run electron:build
```

→ For signed + notarized DMG: `workflows/release.md`
→ For OTA publish: `workflows/deploy.md`
